import Fruit from "./fruit.vue";
import { Api } from "./api";
import { mount, Wrapper } from "@vue/test-utils";

describe("Fruit", () => {
    it("has some unit tests", () => expect(true).toBe(false));
});
